<script>
    let mapa = [
        [1,0,1],
        [0,0,1],
        [0,1,1]
    ];
    function posicao(jogador){
        if(mapa[i][j] == 0){
            //devera andar
        }else{
            //nao podera passar
        }
    }function movimentacao(jogador){
        //função chamada sempre quando o usuario fizer um movimento
    }
    function posicaoinicial(jogador){
        for(let i in mapa[0]){
            if(mapa[0][i] == 0){
                //posição inicial do jogador
            }
        }
    }
</script>
